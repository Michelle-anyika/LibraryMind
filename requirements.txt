# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Configuration
pydantic-settings==2.14.2
python-dotenv==1.2.2

# AI providers
openai==2.43.0
anthropic==0.111.0
google-generativeai==0.8.3

# Vector database  (requires pre-built wheels — skip on Python 3.14+ until wheels exist)
# chromadb==0.5.23

# Caching
redis==5.2.1

# Retry logic
tenacity==9.0.0

# Token counting  (requires Rust compilation — skip on Python 3.14+ until wheels exist)
# tiktoken==0.8.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
